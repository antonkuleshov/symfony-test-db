{% extends 'base.html.twig' %}

{% block title %}Departments{% endblock %}

{% block body %}
    <h1>Departments</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>DeptNo</th>
                <td>{{ department.deptNo }}</td>
            </tr>
            <tr>
                <th>DeptName</th>
                <td>{{ department.deptName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('departments_index') }}">back to list</a>

    <a href="{{ path('departments_edit', {'deptNo': department.deptNo}) }}">edit</a>

    {{ include('departments/_delete_form.html.twig') }}
{% endblock %}
